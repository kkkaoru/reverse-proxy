# shellcheck disable=all
# AWS Account ID (required)
CDK_DEFAULT_ACCOUNT=123456789012

# Target domains to proxy (required)
# Format: protocol:domain,protocol:domain
# Example: https:api.example.com,https:data.example.org
TARGET_DOMAINS=https:api.example.com

# AWS regions to deploy API Gateways (optional)
# Default: us-east-1,us-west-2,eu-west-1,ap-northeast-1
# Available: us-east-1,us-east-2,us-west-1,us-west-2,eu-west-1,eu-west-2,eu-west-3,eu-central-1,ap-northeast-1,ap-northeast-2,ap-southeast-1,ap-southeast-2,ap-south-1,sa-east-1,ca-central-1
REGIONS=us-east-1,eu-west-1,ap-northeast-1

# API Gateway stage name (optional)
# Default: proxy
STAGE_NAME=proxy

# Authentication type (optional)
# Options: api-key, iam
# Default: api-key
AUTH_TYPE=api-key

# ===========================================
# Reverse Proxy Integration (manual configuration only)
# ===========================================

# Authentication type for reverse proxy
# Options: api-key, iam
IP_ROTATE_AUTH_TYPE=api-key

# AWS credentials (required only if IP_ROTATE_AUTH_TYPE=iam)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

# ===========================================
# Example Scripts Configuration (examples/)
# ===========================================

# API Gateway endpoint for example scripts
# Default: https://abc123.execute-api.us-east-1.amazonaws.com/proxy
EXAMPLE_ENDPOINT=

# Request path for example scripts
# Default: /ip
EXAMPLE_PATH=
